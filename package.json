{
  "name": "fruitstock-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && npx prisma generate && npm run build",
    "start": "cd server && npm start"
  }
}
